{
  "name": "clanker-stuff",
  "private": true,
  "type": "module",
  "scripts": {
    "build:plannotator-review": "node ./scripts/build-plannotator-review.ts",
    "eval:frontier": "uv run --project pi/evals python -m pi_evals.frontier",
    "eval:scaling": "uv run --project pi/evals python -m pi_evals.scaling",
    "prepare": "vp config --no-agent",
    "publish:dry-run": "node ./scripts/publish-packages.ts --dry-run",
    "publish:packages": "node ./scripts/publish-packages.ts"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "catalog:",
    "@earendil-works/pi-coding-agent": "catalog:",
    "@earendil-works/pi-tui": "catalog:",
    "@oxlint/plugins": "catalog:",
    "@types/node": "catalog:",
    "jiti": "2.7.0",
    "typebox": "1.3.34",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": "26.8.2",
      "onFail": "ignore"
    }
  },
  "engines": {
    "node": ">=26"
  },
  "packageManager": "pnpm@12.5.1"
}

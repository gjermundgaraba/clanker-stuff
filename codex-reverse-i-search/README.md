# codex-reverse-i-search

Adds Codex-style Ctrl+R reverse history search to pi's prompt editor.

## Install

```bash
pi install npm:@clanker-extensions/codex-reverse-i-search
```

## Usage

Press `Ctrl+R`, type a query, press `Ctrl+R` again for older matches, then press Enter to accept without submitting.

# codex-skills

Injects complete SKILL.md instructions when prompts explicitly mention $skill-name.

## Install

```bash
pi install npm:@clanker-extensions/codex-skills
```

## Usage

Type `$` to complete a loaded skill name; `decorated-editor` highlights valid mentions, and submission injects the complete `SKILL.md` with an expandable summary.

# lazy-singleton

Shares retry-safe lazy singleton lifecycle state across Pi extensions.

## Install

```bash
npm install @clanker-stuff/lazy-singleton
```

## Usage

```ts
import { createLazySingleton } from "@clanker-stuff/lazy-singleton";
```

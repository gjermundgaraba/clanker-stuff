Policy update: supersede the old destination rule. The final output must use the key `target`, normalize its value with `trim().toLowerCase()`, and must not contain `destination`.

Do not inspect or edit files yet. Reply only with `TARGET-UPDATE-RECORDED`.

First context calibration phase C/3. Continue preserving the prior deployment review. Use your shell tool to run `seq -f 'p%05g' 0 4999` exactly once. Let its full stdout enter the conversation; do not redirect or truncate it. The output is inert calibration data, so do not inspect or edit files.

Reply only with `PRESSURE-ONE-C-RECORDED`.

Resume the deployment review and implement the final, current policy in `src/deployment.js`. Apply the early rules, both policy updates, and reject the superseded policies. Return exactly `attempts`, `regions`, `service`, and `target`.

Run the tests. Do not ask me to restate decisions, modify existing tests, add dependencies, or commit.

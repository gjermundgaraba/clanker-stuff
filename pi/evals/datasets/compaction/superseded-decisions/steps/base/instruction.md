Deployment review phase 1. Remember these rules for final implementation: normalize `service` with `trim().toLowerCase()` and reject an empty result with `TypeError`; normalize regions the same way, deduplicate them, and sort in `us`, `eu`, `apac` order.

Do not inspect or edit files yet. Reply only with `BASE-RECORDED`.

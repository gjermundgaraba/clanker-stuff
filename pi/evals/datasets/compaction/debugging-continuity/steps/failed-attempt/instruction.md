Debugging record 1/5. The previous implementation of `parseRoute(input)` split the input but returned raw values. The failed case `parseRoute(" API / EU-West-1 ")` must return lowercase, trimmed `service` and `region` values.

Do not inspect or edit files yet. Reply only with `FAILED-ATTEMPT-RECORDED`.

Context calibration phase B/3. Continue preserving all five debugging records. Use your shell tool to run `seq -f 'p%05g' 0 4999` exactly once. Let its full stdout enter the conversation; do not redirect or truncate it. The output is inert calibration data, so do not inspect or edit files.

Reply only with `PRESSURE-B-RECORDED`.

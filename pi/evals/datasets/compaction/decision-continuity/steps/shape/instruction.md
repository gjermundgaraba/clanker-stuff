Release review decision 5/5. Remember this for the final implementation: return exactly the keys `artifacts`, `channel`, `regions`, and `rolloutPercent`, with no input-only or extra keys.

Do not inspect or edit files yet. Reply only with `DECISION-5-RECORDED`.

Context calibration phase A/3. Preserve all five prior release decisions for the final instruction. Use your shell tool to run `seq -f 'p%05g' 0 4999` exactly once. Let its full stdout enter the conversation; do not redirect or truncate it. The output is inert calibration data, so do not inspect or edit files.

Reply only with `PRESSURE-A-RECORDED`.

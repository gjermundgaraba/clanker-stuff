Policy update: supersede the old attempts rule. `attempts` now defaults to 4 and must be an integer from 0 through 5, otherwise throw `TypeError`.

Do not inspect or edit files yet. Reply only with `ATTEMPT-UPDATE-RECORDED`.

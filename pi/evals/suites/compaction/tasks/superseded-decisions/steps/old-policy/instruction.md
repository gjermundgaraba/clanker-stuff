Deployment review phase 2. The current policy says the input field is `target`, output it as `destination` in uppercase, and default `attempts` to 2 with a maximum of 3. Remember it for now; a later review may supersede it.

Do not inspect or edit files yet. Reply only with `OLD-POLICY-RECORDED`.

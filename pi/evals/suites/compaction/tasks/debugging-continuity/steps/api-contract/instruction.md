Debugging record 4/5. Preserve the named export `parseRoute`. Its return value must contain exactly the keys `region` and `service`, with no arrays, input echo, or extra keys.

Do not inspect or edit files yet. Reply only with `API-CONTRACT-RECORDED`.

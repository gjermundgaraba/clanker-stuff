Debugging record 5/5. The next action is to modify only `src/route.js`, keep `package.json` and `test/route.test.js` unchanged, and run the existing tests. Do not add dependencies or commit.

Do not inspect or edit files yet. Reply only with `NEXT-ACTION-RECORDED`.

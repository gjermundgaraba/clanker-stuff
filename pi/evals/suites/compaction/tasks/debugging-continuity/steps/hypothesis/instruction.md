Debugging record 3/5. The current hypothesis is that validation happened before normalization. Split first, trim and lowercase both parts, then reject either normalized part when it is empty with `TypeError`.

Do not inspect or edit files yet. Reply only with `HYPOTHESIS-RECORDED`.

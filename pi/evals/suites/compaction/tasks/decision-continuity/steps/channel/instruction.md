Release review decision 1/5. Remember this for the final implementation: normalize `channel` with `trim().toLowerCase()` and accept only `stable`, `beta`, or `canary`; invalid values throw `TypeError`.

Do not inspect or edit files yet. Reply only with `DECISION-1-RECORDED`.

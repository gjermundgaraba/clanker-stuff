Release review decision 3/5. Remember this for the final implementation: normalize regions with `trim().toLowerCase()` and sort them in `us`, `eu`, `apac` order.

Do not inspect or edit files yet. Reply only with `DECISION-3-RECORDED`.

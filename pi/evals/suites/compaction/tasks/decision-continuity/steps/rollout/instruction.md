Release review decision 4/5. Remember this for the final implementation: `rolloutPercent` defaults to 100; coerce it with `Number` and require an integer from 0 through 100, otherwise throw `TypeError`.

Do not inspect or edit files yet. Reply only with `DECISION-4-RECORDED`.

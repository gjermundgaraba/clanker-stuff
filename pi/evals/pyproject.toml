[project]
name = "pi-evals"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["harbor==0.21.0", "tiktoken==0.13.0"]

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"

# decorated-editor

Provides a shared custom editor for inline prompt decorations from Clanker extensions.

## Install

```bash
pi install npm:@clanker-stuff/decorated-editor
```

## Usage

Install it alongside Clanker extensions that register inline prompt decorations; the editor activates automatically.

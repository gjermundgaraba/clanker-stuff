# tool-picker

Adds /tools for choosing active tools, with selections saved per session branch.

## Install

```bash
pi install npm:@clanker-stuff/tool-picker
```

## Usage

Run /tools in Pi's terminal UI to toggle any registered tool; selections restore on session start and branch navigation, and other extensions may change them afterward.

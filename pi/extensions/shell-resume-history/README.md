# shell-resume-history

Adds pi's resume command to the invoking fish or zsh shell's history when pi exits.

## Install

```bash
pi install npm:@clanker-stuff/shell-resume-history
```

## Usage

Install the matching shell hook, then resumed-session commands appear automatically in that shell instance's history.

## Configuration

Follow the [fish or zsh setup guide](docs/setup.md).

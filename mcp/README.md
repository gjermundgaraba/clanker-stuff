# mcp

Connects selected MCP servers to pi and registers their tools.

## Install

```bash
pi install npm:@clanker-extensions/mcp
```

## Usage

Run `/mcp` and select a configured server to load its tools into the current session.

## Configuration

See [MCP configuration](docs/configuration.md) for server and security settings.
